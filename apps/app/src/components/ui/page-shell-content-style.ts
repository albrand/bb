import type { CSSProperties } from "react";
import { CONTENT_MEASURE_CSS_VALUE } from "@/lib/content-measure";

export const PAGE_SHELL_CONTENT_STYLE = {
  "--md-content-w": CONTENT_MEASURE_CSS_VALUE,
} as CSSProperties;
